-- colors/Oshen.lua
-- Neovim colorscheme entry point. Loaded automatically by:
--   vim.cmd.colorscheme("Oshen")
-- Call require("oshen").setup({}) BEFORE loading the colorscheme.
require("oshen").load()
